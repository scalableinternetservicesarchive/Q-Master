<%= form_for(@article) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :text, placeholder: "Compose new article...", style: 'color: #000' %>
  </div>
  <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>
